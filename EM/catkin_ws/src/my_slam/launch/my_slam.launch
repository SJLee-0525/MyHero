<?xml version="1.0"?>
<launch>
    <!-- 1) Static Transform Publisher for robot frames -->
    <node pkg="tf" type="static_transform_publisher" name="base_to_laser"
        args="0 0 0 0 0 0 base_link laser_frame 100" />
    
    <node pkg="tf" type="static_transform_publisher" name="base_footprint_to_base"
        args="0 0 0 0 0 0 base_footprint base_link 100" />

    <!-- 2) YDLidar X4 실행 -->
    <include file="$(find ydlidar_ros_driver)/launch/X4.launch" />

    <!-- 3) Hector SLAM 노드 -->
    <node pkg="hector_mapping" type="hector_mapping" name="hector_mapping" output="screen">
        <!-- 프레임 설정 -->
        <param name="map_frame" value="map" />
        <param name="base_frame" value="base_footprint" />
        <param name="odom_frame" value="base_footprint" />
        
        <!-- TF 발행 설정 -->
        <param name="pub_map_odom_transform" value="true" />
        <param name="pub_odometry" value="true" />  <!-- 추가: odometry 토픽 발행 -->
        
        <!-- 스캔 매칭 파라미터 -->
        <param name="use_tf_scan_transformation" value="true" />
        <param name="use_tf_pose_start_estimate" value="false" />
        
        <!-- 업데이트 임계값 조정 -->
        <param name="map_update_distance_thresh" value="0.05" />
        <param name="map_update_angle_thresh" value="0.03" />
        
        <!-- 맵 해상도/크기 -->
        <param name="map_resolution" value="0.050"/>
        <param name="map_size" value="2048"/>
        <param name="map_start_x" value="0.5"/>
        <param name="map_start_y" value="0.5"/>

        <!-- 라이다 Scan 토픽 -->
        <param name="scan_topic" value="/scan"/>
    </node>

    <!-- 4) RViz 실행 (선택적) -->
    <!-- <node pkg="rviz" type="rviz" name="rviz" args="-d $(find my_slam)/rviz/slam.rviz" respawn="true"/> -->
</launch>
