<launch>
  
  <!-- This will launch the costmap_2d node with parameters configured according to gazebo -->
  <node name="costmap_node" pkg="costmap_2d" type="costmap_2d_node" output="screen">
	    <rosparam file="$(find hybrid_astar)/launch/realworld_params.yaml" command="load" ns="costmap" />  
  </node>

  <!-- This will launch the Hybrid A* Path Planner -->
  <node pkg="hybrid_astar" type="hybridAstar_node" name="hybrid_astar_node" clear_params="true">
      <remap from="/base_pose_ground_truth" to="/map"/>
      <remap from="/costmap_node/costmap/costmap" to="/move_base/global_costmap/costmap"/>

      <!-- 글로벌 플랜 결과 토픽을 로컬 플래너가 사용하는 이름으로 remap -->
      <remap from="/astroid_path" to="/plan"/>
  </node>

</launch>